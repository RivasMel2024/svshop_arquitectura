# Variables de entorno para svshop-orders-service (Development local)
NODE_ENV=development
PORT=3001

# MongoDB local (Compass)
# Asegurate de tener el servicio local de MongoDB encendido
MONGO_URI=mongodb://localhost:27017/svshop_orders

# JWT - Debe coincidir con svshop-backend/src/config/.env.development
JWT_SECRET="dev-secret-key-not-for-production"
JWT_EXPIRES_IN=12h

# Email (Nodemailer / SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=rivasmelisa2024@gmail.com
SMTP_PASS=nhxg jvxh fjyf gcli
SMTP_FROM=rivasmelisa2024@gmail.com

# Integración con backend monolito para stock
MONOLITH_API_URL=http://localhost:8080
INTERNAL_API_KEY=svshop-internal-dev-key
